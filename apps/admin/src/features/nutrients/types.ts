/** Flattened, table-friendly view of a contract `Nutrient`. */
export type NutrientRow = {
  id: string;
  code: string;
  name: string;
  description: string;
  unit: string;
  icon: string | null;
};
